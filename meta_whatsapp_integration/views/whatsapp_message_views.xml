<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_whatsapp_message_tree" model="ir.ui.view">
        <field name="name">whatsapp.message.list</field>
        <field name="model">whatsapp.message</field>
        <field name="arch" type="xml">
            <list string="WhatsApp Messages" default_order="create_date desc">
                <field name="create_date"/>
                <field name="partner_id"/>
                <field name="to_number"/>
                <field name="message_type"/>
                <field name="content"/>
                <field name="status" widget="badge" decoration-success="status in ['delivered', 'read']" decoration-warning="status == 'sent'" decoration-danger="status == 'failed'"/>
                <field name="direction"/>
                <field name="business_account_id"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_whatsapp_message_form" model="ir.ui.view">
        <field name="name">whatsapp.message.form</field>
        <field name="model">whatsapp.message</field>
        <field name="arch" type="xml">
            <form string="WhatsApp Message" create="false" edit="false">
                <header>
                    <field name="status" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group name="message_info">
                            <field name="message_id"/>
                            <field name="partner_id"/>
                            <field name="to_number"/>
                            <field name="from_number"/>
                            <field name="business_account_id"/>
                        </group>
                        <group name="message_details">
                            <field name="message_type"/>
                            <field name="template_name"/>
                            <field name="direction"/>
                            <field name="create_date"/>
                        </group>
                    </group>
                    
                    <group string="Message Content">
                        <field name="content" nolabel="1"/>
                    </group>
                    
                      <group string="Error Information" invisible="not error_message">
                    <field name="error_message" nolabel="1"/>
                    </group>

                    <group string="Webhook Data" invisible="not webhook_data">
                        <field name="webhook_data" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_whatsapp_message_search" model="ir.ui.view">
        <field name="name">whatsapp.message.search</field>
        <field name="model">whatsapp.message</field>
        <field name="arch" type="xml">
            <search string="Search WhatsApp Messages">
                <field name="partner_id"/>
                <field name="to_number"/>
                <field name="content"/>
                <filter string="Sent" name="sent" domain="[('status', '=', 'sent')]"/>
                <filter string="Delivered" name="delivered" domain="[('status', '=', 'delivered')]"/>
                <filter string="Failed" name="failed" domain="[('status', '=', 'failed')]"/>
                <filter string="Outbound" name="outbound" domain="[('direction', '=', 'outbound')]"/>
                <filter string="Inbound" name="inbound" domain="[('direction', '=', 'inbound')]"/>
                <filter string="Today" name="today" domain="[('create_date', '>=', datetime.datetime.combine(context_today(), datetime.time(0,0,0)))]"/>
                <group expand="0" string="Group By">
                    <filter string="Status" name="group_status" context="{'group_by': 'status'}"/>
                    <filter string="Direction" name="group_direction" context="{'group_by': 'direction'}"/>
                    <filter string="Message Type" name="group_type" context="{'group_by': 'message_type'}"/>
                    <filter string="Date" name="group_date" context="{'group_by': 'create_date:day'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_whatsapp_message" model="ir.actions.act_window">
        <field name="name">WhatsApp Messages</field>
        <field name="res_model">whatsapp.message</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_today': 1}</field>
    </record>
</odoo>