<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Tree View -->
    <record id="view_whatsapp_template_tree" model="ir.ui.view">
        <field name="name">whatsapp.template.tree</field>
        <field name="model">whatsapp.template</field>
        <field name="arch" type="xml">
            <list string="WhatsApp Templates">
                <field name="name"/>
                <field name="category"/>
                <field name="language"/>
                <field name="status" widget="badge" decoration-success="status == 'APPROVED'" decoration-warning="status == 'PENDING'" decoration-danger="status == 'REJECTED'"/>
                <field name="parameter_count"/>
                <field name="business_account_id"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <!-- Form View -->
    <record id="view_whatsapp_template_form" model="ir.ui.view">
        <field name="name">whatsapp.template.form</field>
        <field name="model">whatsapp.template</field>
        <field name="arch" type="xml">
            <form string="WhatsApp Template">
                <header>
                    <field name="status" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group name="basic_info">
                            <field name="name"/>
                            <field name="template_id"/>
                            <field name="business_account_id"/>
                            <field name="category"/>
                            <field name="language"/>
                            <field name="active"/>
                        </group>
                        <group name="parameters">
                            <field name="parameter_count" readonly="1"/>
                            <field name="parameter_names"/>
                        </group>
                    </group>
                    
                    <notebook>
                        <page string="Template Content">
                            <group>
                                <group string="Header" name="header">
                                    <field name="header_type"/>
                                    <field name="header_text" 
                                        modifiers="{'invisible': [('header_type', '!=', 'TEXT')], 'required': [('header_type', '=', 'TEXT')]}"/>
                                </group>
                                <group string="Body" name="body">
                                    <field name="body_text" nolabel="1"/>
                                </group>
                            </group>
                            <group>
                                <group string="Footer" name="footer">
                                    <field name="footer_text"/>
                                </group>
                            </group>
                        </page>
                        
                        <page string="Buttons">
                            <group>
                                <group name="button_config">
                                    <field name="button_type"/>
                                    <field name="button_text" 
                                        modifiers="{'required': [('button_type', '!=', False)]}"/>
                                    <field name="button_url" 
                                        modifiers="{'invisible': [('button_type', '!=', 'URL')], 'required': [('button_type', '=', 'URL')]}"/>
                                    <field name="button_phone" 
                                        modifiers="{'invisible': [('button_type', '!=', 'CALL_TO_ACTION')], 'required': [('button_type', '=', 'CALL_TO_ACTION')]}"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

<!-- Search View -->
<record id="view_whatsapp_template_search" model="ir.ui.view">
    <field name="name">whatsapp.template.search</field>
    <field name="model">whatsapp.template</field>
    <field name="arch" type="xml">
        <search string="Search WhatsApp Templates">
            <field name="name"/>
            <field name="category"/>
            <filter string="Approved" name="approved" domain="[('status', '=', 'APPROVED')]"/>
            <filter string="Pending" name="pending" domain="[('status', '=', 'PENDING')]"/>
            <filter string="Marketing" name="marketing" domain="[('category', '=', 'MARKETING')]"/>
            <filter string="Utility" name="utility" domain="[('category', '=', 'UTILITY')]"/>
            <filter string="Active" name="active" domain="[('active', '=', True)]"/>
            <group expand="0" string="Group By">
                <filter string="Category" name="group_category" context="{'group_by': 'category'}"/>
                <filter string="Status" name="group_status" context="{'group_by': 'status'}"/>
                <filter string="Language" name="group_language" context="{'group_by': 'language'}"/>
            </group>
        </search>
    </field>
</record>


    <!-- Search View -->
    <record id="view_whatsapp_template_search" model="ir.ui.view">
        <field name="name">whatsapp.template.search</field>
        <field name="model">whatsapp.template</field>
        <field name="arch" type="xml">
            <search string="Search WhatsApp Templates">
                <field name="name"/>
                <field name="category"/>
                <filter string="Approved" name="approved" domain="[('status', '=', 'APPROVED')]"/>
                <filter string="Pending" name="pending" domain="[('status', '=', 'PENDING')]"/>
                <filter string="Marketing" name="marketing" domain="[('category', '=', 'MARKETING')]"/>
                <filter string="Utility" name="utility" domain="[('category', '=', 'UTILITY')]"/>
                <filter string="Active" name="active" domain="[('active', '=', True)]"/>
                <group expand="0" string="Group By">
                    <filter string="Category" name="group_category" context="{'group_by': 'category'}"/>
                    <filter string="Status" name="group_status" context="{'group_by': 'status'}"/>
                    <filter string="Language" name="group_language" context="{'group_by': 'language'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_whatsapp_template" model="ir.actions.act_window">
        <field name="name">WhatsApp Templates</field>
        <field name="res_model">whatsapp.template</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first WhatsApp Message Template
            </p>
            <p>
                Templates must be approved by Meta before they can be used for sending messages.
            </p>
        </field>
    </record>
</odoo>